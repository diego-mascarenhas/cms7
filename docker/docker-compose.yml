services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cms7
    ports:
      - "8080:80"
    volumes:
      - ../:/var/www/html
      - ./000-default.conf:/etc/apache2/sites-available/000-default.conf
